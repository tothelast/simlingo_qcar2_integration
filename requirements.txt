# Core ML/AI dependencies
# Note: Install PyTorch with CUDA using: pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0
torchvision>=0.22.0
torchaudio>=2.7.0
transformers>=4.30.0
huggingface-hub>=0.16.0
pytorch-lightning>=2.4.0

# Model Training & Fine-tuning (PEFT/LoRA)
peft>=0.17.0
einops>=0.8.0
timm>=1.0.0

# Computer vision and image processing
opencv-python>=4.8.0
Pillow>=9.5.0
numpy>=1.24.0

# Configuration Management (Hydra/OmegaConf)
hydra-core>=1.3.0
omegaconf>=2.3.0
pyyaml>=6.0

# Utility libraries
tqdm>=4.65.0
safetensors>=0.6.0
requests>=2.31.0

# Development and testing (optional)
pytest>=7.4.0
loguru>=0.7.0

# Qcar2 and Qlabs dependencies (already available in the system)
# qvl - Quanser Virtual Labs SDK (system installed)
# pal - Quanser PAL library (system installed)
